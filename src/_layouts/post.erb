---
layout: default
---

<article class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
  <!-- Post Header -->
  <header class="mb-10 text-center">
    <time datetime="<%= data.date.to_date %>" class="text-sm text-primary-600 font-medium uppercase tracking-wide">
      <%= data.date.strftime("%B %d, %Y") %>
    </time>
    <h1 class="mt-2 text-4xl font-serif font-bold text-gray-900 leading-tight">
      <%= data.title %>
    </h1>
    <% if data.subtitle %>
      <p class="mt-3 text-xl text-gray-600"><%= data.subtitle %></p>
    <% end %>
  </header>

  <!-- Post Content -->
  <div class="prose prose-lg prose-stone mx-auto prose-headings:font-serif prose-a:text-primary-600 hover:prose-a:text-primary-700">
    <%= yield %>
  </div>

  <!-- Post Footer -->
  <footer class="mt-12 pt-8 border-t border-stone-200">
    <div class="flex items-center justify-between">
      <div>
        <p class="text-sm text-gray-500">Written by</p>
        <p class="font-medium text-gray-900"><%= site.metadata.owner %></p>
      </div>
      <a href="<%= relative_url '/posts' %>" class="btn btn-primary no-underline">
        &larr; All Posts
      </a>
    </div>
  </footer>
</article>
