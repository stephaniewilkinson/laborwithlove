---
layout: default
---

<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
  <h1 class="text-4xl font-serif font-bold text-gray-900 mb-8 text-center"><%= data.title %></h1>

  <div class="prose prose-lg prose-stone mx-auto prose-headings:font-serif prose-a:text-primary-600 hover:prose-a:text-primary-700">
    <%= yield %>
  </div>
</div>
